# Server Configuration
PORT=3000
NODE_ENV=development

# API Configuration
API_VERSION=v1

# CORS Configuration
CORS_ORIGIN=*

# Emergency Buffer Configuration (percentage)
EMERGENCY_BUFFER_PERCENTAGE=0.2

# Average Consultation Time (minutes)
AVG_CONSULTATION_TIME=15

# Slot Duration (minutes)
SLOT_DURATION=60
